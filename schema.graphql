type Query {
  info: String!
  message(id: ID!): Message
  messages: [Message!]!
}

type Mutation {
  createMessage(body: String!, time: String!): Message!
}

type Message {
  id: ID!
  body: String!
  time: String!
}
